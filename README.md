# FoodSto-API

An API that has the products of a grocery store called "FoodSto".

## Getting Started

1. Clone the repository - `git clone git@github.com:moh-ash96/FoodSto-API.git`.

2. Install the Dependencies - `poetry install`.

3. Run the server - `python manage.py runserver`.
